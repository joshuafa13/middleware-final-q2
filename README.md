# Middleware

This is a web app where you can check request time, method, URL and time difference.

## Features

- 輸入網之後 Console 回傳
- 時間戳記 (time-stamps) - 以當地時間 (台北) 顯示
- 請求的 HTTP 方法
- URL
- req & res 的時間差

## Installation & Execution

- run terminal `cmd+space input terminal => enter`
- clone project to your local server `git clone https://github.com/`joshuafa13/middleware-final-q2.git
- go to project folder `cd middleware-final-q2`
- download npm `npm install`
- run with nodemon `npm run dev`
